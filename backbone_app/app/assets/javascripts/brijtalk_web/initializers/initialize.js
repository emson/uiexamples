BrijtalkWeb.Initializers = (function(){

    // _.mixin({

    //  // given two values if the first is undefined or empty 
    //  // return the second, else return the first
    //  lazyOr: function(first, second) {
    //    if( _.isUndefined(first) || _.isEmpty(first) ) {
    //      return second;
    //    }
    //    return first;
    //  }

    // });

});